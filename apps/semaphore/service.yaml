apiVersion: v1
kind: Service
metadata:
  name: semaphore
  namespace: semaphore
spec:
  type: LoadBalancer
  loadBalancerIP: 10.2.1.102
  selector:
    app: semaphore
  ports:
    - port: 80
      targetPort: 3000
